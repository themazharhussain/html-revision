console.log("1")
console.log("2")
console.log("3")
// c, c++, java, python, c-sharp, ruby, rust
// source file
// Way 1 - directly in browser console
// Way 2 - linked file in HTML using script tag
// Way 3 - in terminal using Node.js

//the script tag connects the JavaScript file to the HTML

//NOTE: JavaScr//the script tag connects the JavaScript file to the HTMLipt runs line by line from top to bottom. The HTML must be fully loaded before JavaScript tries to use it. If the script runs first, before the HTML exists, it will not find the page elements and you get errors. Putting the script at the bottom of the body means all the HTML above it is ready before JavaScript runs. Always put your script tag at the bottom of the body.

// Node.js lets JavaScript run outside the browser, directly on your computer. This is how we will build the backend server later. For now just know that JavaScript can run in two places. In the browser for websites, and in Node.js for servers and tools. Same language, two homes. For the next few weeks we will mostly use the browser way.
